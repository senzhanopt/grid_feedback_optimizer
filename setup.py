from setuptools import setup, find_packages

setup(
    name="grid_feedback_optimizer",        # package/distribution name
    version="0.1.0",
    packages=find_packages(),     # automatically find all submodules
    install_requires=[            # dependencies you need for testing or core logic
    ],
    python_requires="==3.11.*"
)